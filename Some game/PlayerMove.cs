using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//Inheriting from TacticsMove , all the private will be used by the player (and can be shared with npc too)
public class PlayerMove : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    { //movement range and move it there and we need the tile class for that 
        
    }
}
